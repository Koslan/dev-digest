import type { Provider } from "../../../../../../../lib/types";

/** Providers a system feature can be pointed at. */
export const PROVIDER_OPTIONS: readonly Provider[] = ["openai", "anthropic", "openrouter"];
